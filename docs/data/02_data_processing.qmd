---
title: data_processing
tbl-colwidths: [25,10,10,45]
jupyter: 
    kernelspec: 
      name: "fm-ml"
      language: "python"
      display_name: "Python-fm-ml"
---

```{python}
#| echo: false
from finmetrika_ml.data import data_processing
from finmetrika_ml.utils import *
from IPython.display import Markdown
```

For classification tasks, or any categorical data feature we can obtain labels with

```{python}
#| echo: false
out = display(Markdown(generate_markdown_doc(data_processing.get_labels)))
```

``` python
data_processing.get_labels(df=my_dataframe,
                           col_label="col_label")
```